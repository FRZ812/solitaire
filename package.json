{
  "name": "solitaire",
  "version": "0.20.0",
  "description": "Solo RPG narrative engine — Vite + React, Supabase backend. The handcrafted map and per-user campaign saves live in Postgres; the editor at #/edit writes back to the same row.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
