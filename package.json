{
  "name": "solitaire",
  "version": "0.20.0",
  "description": "Solo RPG narrative engine — Vite project that builds to a single-file Claude artifact.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:web": "cross-env SOLITAIRE_MODE=web vite",
    "build": "vite build",
    "build:web": "cross-env SOLITAIRE_MODE=web vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "cross-env": "^7.0.3",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.2"
  }
}
