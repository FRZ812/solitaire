node_modules/
dist/
.vite/
*.log
.DS_Store
supabase/.temp/
supabase/functions/.env

# Regenerable verification render (scripts/render-whitemarch.mjs)
scripts/whitemarch-map.png

# Local Claude Code settings (per-machine permissions allowlist, MCP enables, etc.)
.claude/settings.local.json

# Claude Code isolated agent worktrees (per-session scratch — agents work in
# these and the harness reclaims them; never artifacts to commit).
.claude/worktrees/
.wave4-snapshots/

# Local map snapshots and regenerable map renders (DB backups + SVG/PNG
# visualizations produced while editing the Whitemarch map). Same class as
# the ignored render above — local scratch, never repo artifacts.
.map-backups/
scripts/whitemarch-backups/

# Local agent attachments and planning scratch.
.codex-remote-attachments/
.hermes/plans/
.preview-root/

# Godot editor/import caches left by retired engine spikes.
**/.godot/
