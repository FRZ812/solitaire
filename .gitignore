node_modules/
dist/
.vite/
*.log
.DS_Store
.env
.env.*
!.env.example
supabase/.temp/
supabase/functions/.env

# Regenerable verification render (scripts/render-whitemarch.mjs)
scripts/whitemarch-map.png

# Local Claude Code settings (per-machine permissions allowlist, MCP enables, etc.)
.claude/settings.local.json

# Claude Code isolated agent worktrees (per-session scratch — agents work in
# these and the harness reclaims them; never artifacts to commit).
.claude/worktrees/
.wave4-snapshots/

# Local map snapshots and regenerable map renders (DB backups + SVG/PNG
# visualizations produced while editing the Whitemarch map). Same class as
# the ignored render above — local scratch, never repo artifacts.
.map-backups/
scripts/whitemarch-backups/

# Local agent attachments and planning scratch.
.codex-remote-attachments/
.hermes/
.preview-root/

# Godot editor/import caches left by retired engine spikes.
**/.godot/

# Local CLI/tooling debris from environment troubleshooting — never project
# files. Fenced off explicitly because some of these are auth/status dumps and
# one is an executable, and a stray `git add -A` would commit them.
claude-*.txt
claude-wa.exe
claude-npm-cache/
tmp-claude-*/
tmp-npm-cache/
pnpm-alt/
.pnpm-store/
rc-*.txt
rc[0-9]*-debug.txt
envcheck.txt
status-debug-log.txt
System.Management.Automation.Internal.Host.InternalHost

# Local agent/runtime configuration and generated working sets.
.agents/
.codex/
.tmp/
Temp Assets/
NUL
NUL-shm
NUL-wal
**/__pycache__/

# Rendered QA and audit outputs; source harnesses/scripts remain tracked.
tmp/ability-semantic-audit/
tmp/combat-ux-probe/
tmp/*contact-sheet.png
tmp/south-working-fresh-alpha.png

# Explicitly rejected portrait candidate retained only for local comparison.
src/assets/generated/character-portraits/codex-individual/special/threshold-voice-portrait-v2-rejected-colored-outline.png
